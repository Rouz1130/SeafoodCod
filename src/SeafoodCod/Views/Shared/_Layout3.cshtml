

@using System.Security.Claims
<!DOCTYPE html>
<html lang="en" class="no-js">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>@ViewBag.SeafoodCod</title>
    <meta name="author" content="Codrops" />
    <link rel="stylesheet" type="text/css" href="~/css/normalize.css" />
    <link rel="stylesheet" type="text/css" href="~/css/demo.css" />
    <link rel="stylesheet" type="text/css" href="~/css/layout-simple.css" />
    <link rel="stylesheet" type="text/css" href="~/css/styles.css" />
    <!--[if IE]>
    <script src="http://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
</head>
<body>
    <div id="container" class="container">
        <header class="intro">
            <img class="intro__image" src="http://www.capecentralhigh.com/wp-content/uploads/2011/08/Pike-Place-Fish-Market-08-02-2011_2481.jpg" alt="Pike-Place" />
            <div class="intro__content">
                <h1 class="intro__title">Welcome to SeafoodCod</h1>
                <div class="intro__subtitle">
                    <div class="intro__description">
                        <h3>We offer the best Seafood! in the Pacific Northwest</h3>
                        <div class="demos">
                            @if (User.Identity.IsAuthenticated)
                            {
                                <h3>@Html.ActionLink("Logoff", "Logoff", "Account", null, new { @class = "demos__item" })</h3>
                                <h3>@Html.ActionLink("Newsletter", "Create", "Newsletter", null, new { @class = "demos__item" })</h3>

                            }
                            else
                            {
                                <h3>@Html.ActionLink("Home", "Index", "Account", null, new { @class = "demos__item" })</h3>
                                <h3>@Html.ActionLink("Sign Up", "Register", "Account", null, new { @class = "demos__item" })</h3>
                                <h3>@Html.ActionLink("Login", "Login", "Account", null, new { @class = "demos__item" })</h3>
                                <h3>@Html.ActionLink("Newsletter", "Index", "Newsletter", null, new { @class = "demos_item" })</h3>
                            }


                        </div>
                    </div>
                    <button class="trigger">
                        <svg width="100%" height="100%" viewBox="0 0 60 60" preserveAspectRatio="none">
                            <g class="icon icon--grid">
                                <rect x="32.5" y="5.5" width="22" height="22" />
                                <rect x="4.5" y="5.5" width="22" height="22" />
                                <rect x="32.5" y="33.5" width="22" height="22" />
                                <rect x="4.5" y="33.5" width="22" height="22" />
                            </g>
                            <g class="icon icon--cross">
                                <line x1="4.5" y1="55.5" x2="54.953" y2="5.046" />
                                <line x1="54.953" y1="55.5" x2="4.5" y2="5.047" />
                            </g>
                        </svg>
                        <span>View content</span>
                    </button>
                </div>
            </div><!-- /intro__content -->
        </header><!-- /intro -->
        @RenderBody()
    </div><!-- /container -->



    <script src="~/js/classie.js"></script>
    <script>
			(function() {
				var container = document.getElementById( 'container' ),
					trigger = container.querySelector( 'button.trigger' );

				function toggleContent() {
					if( classie.has( container, 'container--open' ) ) {
						classie.remove( container, 'container--open' );
						classie.remove( trigger, 'trigger--active' );
						window.addEventListener( 'scroll', noscroll );
					}
					else {
						classie.add( container, 'container--open' );
						classie.add( trigger, 'trigger--active' );
						window.removeEventListener( 'scroll', noscroll );
					}
				}

				function noscroll() {
					window.scrollTo( 0, 0 );
				}

				// reset scrolling position
				document.body.scrollTop = document.documentElement.scrollTop = 0;

				// disable scrolling
				window.addEventListener( 'scroll', noscroll );

				trigger.addEventListener( 'click', toggleContent );

				// For Demo purposes only (prevent jump on click)
				[].slice.call( document.querySelectorAll('.items-wrap a') ).forEach( function(el) { el.onclick = function() { return false; } } );
			})();
    </script>
</body>
</html>